---
name: innodb-redo-log
description: Explain InnoDB Redo Log (why it exists, what it records, sn/lsn/offset organization, write path with writer/flusher/commit-wait, checkpointing, and crash recovery) based primarily on CatKang's MySQL 8.0 deep dive and supplemented by InnoDB Kernel Vol.1.
---

> 目的：当用户询问 **为什么需要 REDO、REDO 记录了什么、REDO 如何组织/落盘、写入链路/刷盘/Checkpoint/恢复** 时，输出 **工程可落地** 的解释：术语表 + 图（Mermaid/文章图片）+ 伪码/模块拆分 + 排障要点。  
> 主参考：CatKang《庖丁解 InnoDB 之 REDO LOG》（基于 MySQL 8.0）  
> 辅参考：《MySQL 内核：InnoDB 存储引擎（卷一）》第 4 章（重做日志）与第 5 章（Mini-Transaction）

---

## 0. 强制输出格式（必须遵守）

### 0.1 先给结论，再给结构
每次回答必须按顺序输出：
1) **一句话结论**（回答“是什么/为什么”）  
2) **关键点列表**（3～8 条）  
3) **图**（至少一张：Mermaid 或文章图片）  
4) （如问实现）给 **伪码 / 模块拆分 / 测试点**

### 0.2 统一术语（全局一致）
- `sn`：仅 REDO 数据字节流偏移（不含 log block 头尾开销）  
- `lsn`：Log Sequence Number（包含 log block 开销），用于恢复幂等与定位  
- `offset`：映射到 redo 文件组（逻辑大文件）的物理偏移  
- `commit_lsn`：某次 mtr/group 的结束 lsn（提交点）  
- `write_lsn`：已写入 redo 文件（通常到 OS page cache）  
- `flushed_lsn`：已 fsync 持久化到磁盘  
- `checkpoint_lsn`：已确保对应脏页落盘，可回收 redo 的边界  
- `page_lsn`：页头已应用最大 lsn（用来判断是否跳过 redo）

> 版本提醒：本 skill 以 MySQL 8.0 机制（sn/lsn/offset、recent_written/recent_closed、notifier、dpa_lsn）为主；涉及 MySQL 8.0.30+ “dynamic redo 文件布局/容量参数”时需要额外提示差异。

---

## 1. 为什么需要记录 REDO（必答）

### 1.1 一句话结论
为了在 **Buffer Pool 延迟刷盘** 的前提下仍能保证崩溃恢复与持久性，InnoDB 采用 **WAL（Write-Ahead Logging）**：先把对 Page 的修改写入 REDO，再允许对应 Page 晚写回数据文件。

### 1.2 必须覆盖的要点（最少 4 条）
- Page 修改发生在内存；崩溃会丢内存中的改动  
- 只要 redo 已持久化，崩溃后就能通过 **重放 redo** 恢复到崩溃前状态  
- 提交需要等待 redo 达到某个持久化水位（取决于 flush 策略）  
- redo 让数据文件可以“批量/合并/延迟”写，充分利用顺序写性能

### 1.3 文章配图（WAL 直觉）
（如用户需要“直观图”，可引用文章中的整体示意图/章节图；若文章无单独 WAL 图，可用下方 Mermaid）

```mermaid
flowchart LR
  A[Modify Page in Buffer Pool] --> B[Generate Redo Record]
  B --> C[Append to Log Buffer]
  C --> D[Write Redo File]
  D --> E[fsync -> Durable]
  A --> F[Dirty Page Flush Later]
  E -->|Crash| G[Recovery: Redo Replay]
```

---

## 2. REDO 中记录了什么内容（必答）

### 2.1 一句话结论
Redo 采用 **Physiological Logging**：**以 Page 为单位**，但在 **页内用逻辑方式**描述修改（不是 SQL、不是整页镜像）。

### 2.2 “需要什么样的 REDO”三要求（必须提）
- **数据量小**（直接影响提交延迟与吞吐）  
- **幂等/可重入**（恢复时不知道哪些页已落盘）  
- **基于 Page**（便于并发重放，加快恢复）

### 2.3 Redo 类型三大类（按作用对象）
- **Page REDO**：对 Index/Undo/Rtree 等页面的修改（占绝大多数）  
- **Space REDO**：文件/空间级操作（create/delete/rename 等，恢复主要用于校验）  
- **Logic REDO**：辅助/边界信息（例如标记一个 redo group 结束）

### 2.4 文章核心例子：UPDATE_IN_PLACE 的字段布局（必须会讲）
> 这张图最能落地说明 “redo 里到底写了什么”： (Type, SpaceId, PageNo) + 页内定位 + 修改字段数组。

![](https://catkang.github.io/assets/img/innodb_redo/redo_insert.png)

回答时必须点名字段含义：
- `Type`：日志类型（例：`MLOG_REC_UPDATE_IN_PLACE`）  
- `Space ID + Page Number`：唯一定位 Page  
- `Record Offset`：目标 Record 在 Page 内偏移  
- `Update Field Count`：修改字段数  
- 每个 field：`Field Number + Field Data Length + Field Data`（修改后的值）

### 2.5 文章例子：Space REDO（FILE_CREATE）的字段布局
![](https://catkang.github.io/assets/img/innodb_redo/redo_space.png)

回答要点：
- `Page Number` 常为 0（空间级）  
- 记录 file flags、file name 等信息（用于恢复/校验）

### 2.6 幂等的关键：page_lsn
- 每个 redo record 有递增 `lsn`  
- Page 修改时会把对应 `lsn` 写到页头 `page_lsn`  
- 恢复时：如果 `redo_lsn <= page_lsn` 就跳过（保证幂等）

---

## 3. REDO 是如何组织的（必答，必须给三层与图）

> 组织 = “redo 内容如何变成磁盘文件字节”的层次结构。按 CatKang：**sn → lsn → offset**。

### 3.1 逻辑层：Logic Redo（sn）
- redo records 先拼成一条连续字节流  
- 用 `sn` 表示“redo 数据本体”的逻辑偏移（不计 log block 头尾开销）

![](https://catkang.github.io/assets/img/innodb_redo/logic_redo.png)

### 3.2 物理层：Log Block（lsn）
- redo 以 **512B log block** 为最小物理组织单位  
- 每个 block：**Header + Data + Trailer(Checksum)**  
- 由于 header/trailer 开销，物理偏移用 `lsn`（计入开销）表示

![](https://catkang.github.io/assets/img/innodb_redo/image-20200216201419532.png)

回答必须覆盖这些字段：
- `Flush Flag`、`Block Number`  
- `Data Length`  
- `First Record Offset`（支持从任意 block 找到合法 record 起点）  
- `Checkpoint Number`（配合循环写）  
- `Trailer Checksum`（校验完整写入）

> 图中标注 “Total space available: 496 bytes”，因此工程实现中 **每块可承载 redo 数据为 496B**（512-12-4）。

### 3.3 record 如何放进 block：可跨块
- block 可包含多个 record  
- record 也可能跨多个 block

![](https://catkang.github.io/assets/img/innodb_redo/physical_redo.png)

### 3.4 sn 与 lsn 的换算（回答给思路即可）
- `sn` 只算 redo 数据  
- `lsn` = `sn` +（前面 block 的头尾开销）+（当前 block header 偏移）  
- 本质：按 block 计数，把开销叠加进来

### 3.5 文件层：Redo Files（offset）
- `lsn` 最终映射到 redo 文件组的 `offset`  
- 多个 redo 文件逻辑上拼成一个大环形空间  
- 每个文件开头预留 block 存储 header/checkpoint 信息

文件预留区：
![](https://catkang.github.io/assets/img/innodb_redo/file_header.png)

Header block 内容：
![](https://catkang.github.io/assets/img/innodb_redo/redo_file_header.png)

三层总览：
![](https://catkang.github.io/assets/img/innodb_redo/redo_file.png)

回答要点：
- 通过维护 `(current_file_real_offset, current_file_lsn)` 可把 `lsn` 转成 `offset`：  
  `real_offset = current_file_real_offset + (lsn - current_file_lsn)`

---

## 4. 写路径时序图（必备模板：mtr_commit → writer → flusher → commit-wait）

```mermaid
sequenceDiagram
  autonumber
  participant U as User Thread
  participant M as MTR
  participant LB as Log Buffer
  participant W as Log Writer
  participant F as Log Flusher
  participant N as Notifier

  U->>M: mtr_commit()
  M->>LB: reserve + copy redo group <br/>(get commit_lsn)
  LB-->>U: commit_lsn

  W->>LB: consume published continuous ranges
  W-->>W: advance write_lsn

  F->>W: wait/notice write_lsn
  F-->>F: fsync -> advance flushed_lsn

  alt innodb_flush_log_at_trx_commit=1
    U->>N: wait(flushed_lsn >= commit_lsn)
    N-->>U: wakeup -> commit success (durable)
  else innodb_flush_log_at_trx_commit=2
    U->>N: wait(write_lsn >= commit_lsn)
    N-->>U: wakeup -> commit success (OS cache)
  else innodb_flush_log_at_trx_commit=0
    U-->>U: return success (periodic flush)
  end
```

---

## 5. 高并发写入关键机制（8.0 主线）

### 5.1 无锁预留：并发写 Log Buffer
- 全局维护 `sn`  
- 线程写入前用原子 `sn.fetch_add(len)` 预留独占区间  
- 线程并行把 redo group 拷贝到各自区间

### 5.2 处理“空洞”：recent_written（link_buf）保证连续写
- 并发预留会出现：某个区间已预留但尚未写完/发布  
- writer 只能写入“已连续完成”的部分  
- `recent_written` 记录每段完成信息，writer 通过推进 tail 找到连续区间并更新 `write_lsn`

### 5.3 降低唤醒成本：notifier
- 大量事务 commit-wait 会产生大量唤醒  
- 使用 write/flush notifier 专门做唤醒，避免污染 writer/flusher 关键路径

---

## 6. Checkpoint 推进（flush list → checkpoint_lsn → free space）

### 6.1 一句话结论
Checkpoint 的作用是推进 `checkpoint_lsn`，从而回收 redo 环形空间（free space 增加），避免 redo 空间不足导致用户线程 stall。

### 6.2 8.0 的安全 checkpoint：min(lwm_lsn, dpa_lsn)
- `lwm_lsn`：未落盘脏页的最老 LSN（low-watermark）  
- `dpa_lsn`：截至该 LSN，脏页已被“登记进 flush tracking/flush list”  
- `checkpoint_lsn = min(lwm_lsn, dpa_lsn)`：避免“redo 已写但脏页未登记”窗口导致推进过头

### 6.3 时序图模板（必须会输出）
```mermaid
sequenceDiagram
  autonumber
  participant FL as Flush List
  participant PC as Page Cleaner
  participant CP as CheckpointMgr
  participant LS as LogSys

  CP->>LS: free = cap - (current_lsn - checkpoint_lsn)
  alt free low
    CP->>PC: aggressive flush
  else normal
    CP->>PC: steady flush
  end

  PC->>FL: flush oldest dirty pages
  FL-->>CP: lwm_lsn moved forward

  CP->>LS: advance checkpoint_lsn = min(lwm_lsn, dpa_lsn)
  LS-->>CP: free space increased
```

---

## 7. Crash Recovery（重放与幂等）

必须覆盖：
- 从 `checkpoint_lsn` 开始扫描 redo（到最新）  
- 对每条 redo：  
  - 若 `redo_lsn <= page_lsn`：跳过（幂等）  
  - 否则 apply  
- Page 可能 partial write：需要 doublewrite 保证拿到正确基页，再 apply redo（卷一/常识补充）

---

## 8. 工程实现（给自研/mini-db）

### 8.1 MVP 模块拆分（推荐）
- `RedoLogCore`：维护水位/配置/线程生命周期
- `RedoRecordCodec`：type/space/page + payload 编码解码
- `LogBuffer`：reserve/copy/publish（先单线程，再并发预留）
- `LogWriter`：log buffer → redo files（推进 `write_lsn`）
- `LogFlusher`：fsync（推进 `flushed_lsn`）
- `CheckpointManager`：协同刷脏推进 `checkpoint_lsn`
- `Recovery`：scan/parse/apply

### 8.2 最小 API（回答实现问题直接给）
- `append_group(records) -> commit_lsn`
- `write_up_to(lsn)`（推进 write_lsn）
- `flush_up_to(lsn)`（推进 flushed_lsn）
- `wait_durable(commit_lsn, policy)`
- `advance_checkpoint()`
- `recover_from(checkpoint_lsn)`

### 8.3 故障注入/测试点（必须给）
- 写完 log buffer 未写文件即 crash  
- 写到 OS cache 未 fsync 即 crash  
- fsync 后 crash（应完全可恢复）  
- 页半写（基页损坏 + redo apply）  
- redo 空间不足触发反压/强刷脏

---

## 9. 常见问答（快速模板）

- **Q：Redo 和 Undo 的区别？**  
  A：Redo 用于崩溃恢复的“前滚”（保证 D），Undo 用于回滚/MVCC（保证 A 与一致读）。

- **Q：为什么 redo 不直接记录整页？**  
  A：整页会放大写入量；redo 追求小、可幂等，并以 page 为单位但页内用逻辑记法（physiological logging）。

- **Q：为什么需要 sn/lsn 两套？**  
  A：sn 方便表示“纯 redo 数据流”；lsn 需要把 log block 头尾开销算进去，才能和物理 block/文件偏移一致。

## 10. 一次事两次提交关于redlog的时序图
```mermaid
sequenceDiagram
autonumber
participant C as Client
participant S as SQL Layer
participant I as InnoDB (row/idx)
participant M as MTR
participant BP as Buffer Pool
participant U as Undo Pages
participant LB as Log Buffer
participant W as Log Writer
participant F as Log Flusher
participant N as Notifier (commit-wait)

C->>S: BEGIN
S->>I: start trx (trx_id)

%% insert row #1
C->>S: INSERT row#1
S->>I: row_insert(row#1)
I->>M: mtr_start()
I->>BP: latch & get clustered index leaf page
I->>U: write undo record (for rollback/MVCC)
I->>BP: insert record into clustered index page
opt secondary indexes exist
I->>BP: insert into secondary index pages
end
I->>M: collect redo records (page patches)
M->>LB: mtr_commit(): append redo group#1 -> commit_lsn1
LB-->>I: commit_lsn1

%% insert row #2
C->>S: INSERT row#2
S->>I: row_insert(row#2)
I->>M: mtr_start()
I->>BP: latch & get clustered index leaf page
I->>U: write undo record
I->>BP: insert record into clustered index page
opt secondary indexes exist
I->>BP: insert into secondary index pages
end
I->>M: collect redo records (page patches)
M->>LB: mtr_commit(): append redo group#2 -> commit_lsn2
LB-->>I: commit_lsn2

%% commit
C->>S: COMMIT
S->>I: trx_commit()
I->>N: commit-wait (policy dependent)

W->>LB: consume published continuous ranges
W-->>W: advance write_lsn

F->>W: wait/notice write_lsn
F-->>F: fsync -> advance flushed_lsn

alt innodb_flush_log_at_trx_commit=1
N-->>I: flushed_lsn >= commit_lsn2
I-->>S: commit ok (durable)
else =2
N-->>I: write_lsn >= commit_lsn2
I-->>S: commit ok (OS cache)
else =0
I-->>S: commit ok (periodic flush)
end
S-->>C: OK
```

---

## References
- CatKang：庖丁解 InnoDB 之 REDO LOG  
  https://catkang.github.io/2020/02/27/mysql-redo.html  
- 《MySQL 内核：InnoDB 存储引擎（卷一）》：第 4 章（重做日志）、第 5 章（Mini-Transaction）
