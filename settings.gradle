rootProject.name = 'miniproject'

include 'mini-mysql'
include 'mini-spring'
include 'mini-tomcat'
include 'mini-nyadbbyjava'
include 'mini-simplebookdb'
include 'mini-db'


rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
}

